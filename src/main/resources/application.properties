spring.application.name=Task_Manager

#  SERVER CONFIG
# ===============================
server.port=8080

# ===============================
#  DATABASE CONFIG
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/taskmanager_db2?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=HAri7510@

# ===============================
#  JPA CONFIG
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=harikrishnae7510@gmail.com
spring.mail.password=zmoo avwc esgg rhuj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Max upload size (Spring Boot)
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# Optional: increase MySQL max_allowed_packet if storing large blobs
# (configure in MySQL server config)
